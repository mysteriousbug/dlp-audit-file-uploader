ncident_impact_data = []
for impact, count in rca_results['patterns']['impact_priority']['incident_impact'].items():
    incident_impact_data.append({
        'Category': 'Incident Impact',
        'Value': impact,
        'Count': count,
        'Percentage': round(count/len(df)*100, 1)
    })

change_impact_data = []
for impact, count in rca_results['patterns']['impact_priority']['change_impact'].items():
    change_impact_data.append({
        'Category': 'Change Impact',
        'Value': impact,
        'Count': count,
        'Percentage': round(count/len(df)*100, 1)
    })

impact_data = incident_impact_data + change_impact_data
impact_df = pd.DataFrame(impact_data)
